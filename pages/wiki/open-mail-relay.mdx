import { TutorialLayout, Notice } from "../../components";

export const meta = {
  title: "Open Mail Relay",
  date: "2018-02-02T12:35:12+01:00",
  LastModifierDisplayName: "Paál Gyula",
};

An open mail relay is an _SMTP_ server configured in such a way that it allows anyone on the Internet to send e-mail through it, not just mail destined to or originating from known users. This used to be the default configuration in many mail servers; indeed, it was the way the Internet was initially set up, but open mail relays have become unpopular because of their exploitation by spammers and worms. Many relays were closed, or were placed on blacklists by other servers.

Further information: [Wikipedia](https://en.wikipedia.org/wiki/Open_mail_relay)

export default ({ children }) => (
  <TutorialLayout meta={meta}>{children}</TutorialLayout>
);
